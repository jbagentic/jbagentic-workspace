# Acme Platform

Internal backend monorepo. Each folder documents itself — read a folder's README
before changing it, and keep that README true when you change the folder.

- [services/](services/) — the application services.
