# DEPRECATED — predates the current retry policy and is no longer imported
# anywhere. Kept only for historical reference. See README.md for the policy
# that actually applies.

RETRY_ATTEMPTS = 5         # WRONG / stale — do not use
RETRY_DELAY = "fixed"      # 10s between every attempt — stale
