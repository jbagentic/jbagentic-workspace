# Widget Config

Fields the exporter reads when rendering a widget.

| Field | Required | Notes |
|-------|----------|-------|
| `id` | yes | widget identifier |
| `format` | yes | `png` or `svg` |
| `width` | no | pixels; default 800 |
| `height` | no | pixels; default 600 |
